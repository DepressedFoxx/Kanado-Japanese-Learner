# Chạy ở máy: copy file này thành .env.local
# Bản deploy: giá trị nằm trong .env.production (đã commit, vì URL không phải bí mật)
NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# Server-only: không thêm NEXT_PUBLIC_ vào tên khóa.
GOOGLE_TTS_API_KEY=""
GOOGLE_TTS_VOICE="ja-JP-Neural2-B"
